#include "Mesh.hpp"

// Mesh.hpp now provides compatibility aliases to the active SurfaceImport.hpp
// model. This translation unit is intentionally empty because the active M16
// surface model is header-only.
